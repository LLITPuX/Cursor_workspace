# 🧠 Cursor Workspace with FalkorDB Memory

**Репозиторій для роботи з Cursor AI та системою пам'яті на базі FalkorDB.**

Цей воркспейс містить:
- **FalkorDB Service** — локальна графова база даних для зберігання сесій та графу знань
- **QPE Service** — Query Processing Engine для обробки запитів та генерації embeddings
- **Граф знань** — система правил, інструкцій та протоколів для AI агента
- **RAG (Retrieval-Augmented Generation)** — векторний пошук для знаходження релевантної інформації

---

## 🎯 Філософія проекту

Цей проект реалізує систему пам'яті для AI агента на основі графу знань:

- **Правила (Rules)** — фундаментальні принципи роботи системи
- **Інструкції (Instructions)** — покрокові керівництва для конкретних задач
- **Протоколи (Protocols)** — стандартизовані набори інструкцій з триггерами
- **Граф знань** — зв'язки між правилами, інструкціями та протоколами через графові зв'язки
- **RAG (Retrieval-Augmented Generation)** — векторний пошук для знаходження релевантної інформації

Система дотримується принципу **Append-Only**: дані ніколи не видаляються, тільки архівуються.

📖 **Детальна філософія:** [`PHILOSOPHY.md`](PHILOSOPHY.md)

---

## 📁 Структура проекту

```
C:\Cursor workspace\
├── falkordb-service/        # 🗄️ Сервіс FalkorDB
│   ├── app/                 # FastAPI додаток
│   │   ├── api/             # API endpoints
│   │   ├── embedding.py     # Генерація embeddings через Ollama
│   │   └── main.py          # Точка входу
│   ├── scripts/             # Утиліти та скрипти
│   ├── docker-compose.yml   # Docker конфігурація
│   └── README.md            # Документація falkordb-service
│
├── scripts/                 # 📜 Скрипти FalkorDB
│   ├── db_cli.py            # CLI для інгестії та пошуку
│   ├── ingest_session.py    # Інгестія сесій у граф
│   ├── search_memory.py     # Пошук у пам'яті через QPE
│   └── clear_graph.py       # Очищення графу
│
├── saved_sessions/          # 💾 Збережені сесії (markdown файли)
│
├── AGENT.md                 # Правила для AI агента
├── PHILOSOPHY.md            # Філософія проекту
├── ROADMAP_FALKORDB_QPE.md  # Дорожня карта проекту
└── README.md                # Цей файл
```

---

## 🚀 Швидкий старт

### 1. Запуск FalkorDB Service

#### Варіант A: Docker (рекомендовано)

```bash
cd falkordb-service

# Запуск сервісів
docker-compose up -d

# Завантаження моделі Ollama для embeddings
docker exec -it falkordb-ollama ollama pull embeddinggemma:latest

# Перевірка
curl http://localhost:8000/api/v1/qpe/health
```

#### Варіант B: Локально

```bash
cd falkordb-service

# Встановіть залежності
pip install -r requirements.txt

# Запустіть Ollama локально
ollama pull embeddinggemma:latest

# Запуск сервісу
python -m app.main
```

---

## 🗄️ FalkorDB Service

Сервіс для роботи з графовою базою даних FalkorDB та обробки запитів через QPE.

### Основні можливості:

- ✅ Генерація embeddings через Ollama (EmbGEMMA)
- ✅ Query Processing Engine (QPE) для обробки запитів
- ✅ Збереження сесій та повідомлень у FalkorDB
- ✅ Граф знань — робота з правилами, інструкціями та протоколами
- ✅ Класифікація запитів (Sentiment, Intent, Complexity)
- ✅ Вилучення сутностей (GLINER)

### API Endpoints:

#### QPE (Query Processing Engine)
- `POST /api/v1/qpe/process-query` — обробка запиту користувача
- `POST /api/v1/qpe/process-assistant-response` — обробка відповіді агента
- `GET /api/v1/qpe/health` — перевірка здоров'я сервісу

📖 **Детальна документація:** [`falkordb-service/README.md`](falkordb-service/README.md)

---

## 📚 Робота з графом знань

### Критичні правила системи

Система містить критичні правила, які завантажуються автоматично:

1. **AlwaysConfirmBeforeChanges** - ЗАВЖДИ отримуй явне підтвердження перед внесенням змін
2. **AppendOnlyPrinciple** - Ніколи не видаляй дані, тільки архівуй
3. **EntityRelationships** - Завжди створюй зв'язки через граф
4. **SessionContextSave** - Зберігай повідомлення з embeddings та зв'язками
5. **SourcePriority** - Пріоритет джерел інформації
6. **TemporalFacts** - Темпоральність для всіх фактів
7. **UseExistingTools** - Перевіряй наявність існуючих рішень
8. **VerifyBeforeAct** - Перевіряй існування перед згадуванням

---

## 🗄️ Структура даних у FalkorDB

Опис схеми графа (вузли, зв'язки, типи даних) буде додано незабаром.

---

## 📝 Скрипти та утиліти

---

## 🛠 Технології

- **FalkorDB** — локальна графова база даних
- **Ollama** + **EmbeddingGemma** — локальна генерація embeddings
- **FastAPI** — REST API сервіс
- **Docker** — контейнеризація
- **Cypher** — мова запитів для графу

---

## 🔧 Розробка

### Додавання нових endpoints

1. Додайте endpoint в `falkordb-service/app/api/routes.py`
2. Додайте методи в `falkordb-service/app/embedding.py` якщо потрібно
3. Перезапустіть сервіс

### Тестування

```bash
# Health check
curl http://localhost:8000/api/v1/qpe/health

# Обробка запиту
curl -X POST http://localhost:8000/api/v1/qpe/process-query \
  -H "Content-Type: application/json" \
  -d '{"query": "Test query"}'

# Документація API
# Відкрийте http://localhost:8000/docs
```

---

## 📄 Ліцензія

MIT License
