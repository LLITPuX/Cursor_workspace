# 🧠 Філософія системи пам'яті Cursor AI

## Основні принципи

### 1. Граф знань (Knowledge Graph)
Система використовує граф знань для зберігання та зв'язування інформації:
- **Вузли** — правила, інструкції, протоколи, факти, сесії, повідомлення
- **Ребра** — зв'язки між вузлами з типами відношень
- **Темпоральність** — версіонування через `valid_from`/`valid_to`

### 2. Ієрархія знань
```
SystemNode (CriticalRules)
  └─ Rule (фундаментальні принципи)
      └─ Instruction (використовує правила)
          └─ Protocol (містить інструкції)
```

### 3. Append-Only принцип
Дані ніколи не видаляються, тільки архівуються:
- При зміні факту: закривається старий (`valid_to = NOW()`), створюється новий
- Повна історія доступна для аналізу та відновлення

### 4. RAG (Retrieval-Augmented Generation)
Векторний пошук для знаходження релевантної інформації:
- Пошук в історії сесій (`Message` вузли)
- Пошук в графі знань (`Rule`, `Instruction`, `Protocol` вузли)
- Використання similarity scores для ранжування
- Генерація embeddings через QPE Service

### 5. Автоматизація через протоколи
Протоколи запускаються автоматично через тригери:
- Аналіз контексту запиту
- Пошук збігів в тригерах протоколів
- Автоматичне виконання протоколу

### 6. Безпека змін
Система дотримується принципу обов'язкового підтвердження:
- ЗАВЖДИ отримуй явне підтвердження перед внесенням змін
- Це правило діє незалежно від режиму роботи
- У ask mode показуй зміни для копіювання (MCP інструменти недоступні)
- У agent mode також завжди питай підтвердження перед використанням інструментів

## Типи сутностей

### Правила (Rule)
Короткі, недвозначні вимоги:
- Приклад: "Дотримуйся принципу Append-Only"
- Зберігаються як вузли типу `Rule`
- Зв'язані з CriticalRules через зв'язок `CONTAINS`

### Інструкції (Instruction)
Набір правил для конкретного типу задач:
- Приклад: "Виконай векторний пошук через QPE: 1) Оброби запит через QPE, 2) Шукай подібні повідомлення"
- Зберігаються як вузли типу `Instruction`
- Використовують правила через зв'язок `USES`

### Протоколи (Protocol)
Стандартизований набір інструкцій:
- Запускається через тригери
- Зберігаються як вузли типу `Protocol`
- Містять інструкції через зв'язок `CONTAINS`

## Цикл роботи

1. **Завантаження правил** — при першому запиті завантажуються CriticalRules
2. **Пошук інформації** — векторний пошук через QPE для знаходження релевантних інструкцій
3. **Завантаження інструкцій** — завантаження пов'язаних інструкцій та правил
4. **Перевірка протоколів** — визначення чи потрібно застосувати SafeChangeProtocol
5. **Пропозиція та підтвердження** — якщо потрібні зміни, покажи їх та запитай підтвердження
6. **Виконання** — застосування інструкцій для формування відповіді (після підтвердження)
7. **Збереження** — збереження сесії з зв'язками до використаних правил/інструкцій у FalkorDB

## Переваги підходу

- **Масштабованість** — легко додавати нові правила та інструкції
- **Відстеження** — повна історія використання правил та інструкцій
- **Гнучкість** — можливість версіонування та архівування
- **Автоматизація** — протоколи запускаються автоматично
- **RAG** — векторний пошук для знаходження релевантної інформації
- **Локальність** — всі дані зберігаються локально в FalkorDB

## Приклади використання

### Обробка запиту через QPE
```bash
POST /api/v1/qpe/process-query
{
  "query": "Як працювати з базою даних?"
}
```

### Пошук подібних повідомлень
Використовуй embeddings з QPE для пошуку подібних повідомлень у графі через векторний пошук.

### Створення зв'язків
```cypher
MATCH (m:Message {id: $message_id})
MATCH (r:Rule {id: $rule_id})
CREATE (m)-[:USES]->(r)
```
