# 🧠 Філософія системи пам'яті Cursor AI

## Основні принципи

### 1. Граф знань (Knowledge Graph)
Система використовує граф знань для зберігання та зв'язування інформації. **Опис схеми графа (вузли, зв'язки, типи) буде додано незабаром.**

### 2. Append-Only принцип
Дані ніколи не видаляються без явного дозволу; зміни архівуються. Повна історія доступна для аналізу та відновлення.

### 3. RAG (Retrieval-Augmented Generation)
Векторний пошук для знаходження релевантної інформації через QPE Service (embeddings та similarity).

### 4. Автоматизація через протоколи
Протоколи запускаються через тригери; контекст запиту аналізується для вибору відповідного протоколу.

### 5. Безпека змін
- ЗАВЖДИ отримуй явне підтвердження перед внесенням змін
- У ask mode показуй зміни для копіювання
- У agent mode також питай підтвердження перед використанням інструментів

## Цикл роботи

1. Завантаження правил з графа (згідно з актуальною схемою — опис буде додано незабаром)
2. Пошук інформації через QPE
3. Завантаження релевантних інструкцій
4. Перевірка протоколів (наприклад, SafeChangeProtocol)
5. Пропозиція та підтвердження змін
6. Виконання після підтвердження
7. Збереження сесії в FalkorDB згідно зі схемою графа

## Переваги підходу

- Масштабованість, відстеження, гнучкість
- RAG для релевантної інформації
- Локальність — дані в FalkorDB

## Приклади використання

### Обробка запиту через QPE
```bash
POST /api/v1/qpe/process-query
{
  "query": "Як працювати з базою даних?"
}
```

Пошук у пам'яті: використовуй `python scripts/db_cli.py "<запит>"` або QPE для векторного пошуку. Деталі роботи з графом (типи вузлів, зв'язки) будуть описані незабаром.
