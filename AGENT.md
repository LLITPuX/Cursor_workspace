# 🧠 Система Пам'яті FalkorDB (Bootstrap Protocol)

## 🎯 Основна ідея
Твої детальні інструкції знаходяться в графовій базі даних FalkorDB. Це правило — лише "бутстрап" для доступу до них.

## 🔄 Обов'язковий цикл для КОЖНОГО запиту

### Крок 1: Завантаження критичних правил (MANDATORY - завжди)
При першому запиті до бази:
1. Знайди вузол "CriticalRules" в графі:
   ```cypher
   MATCH (n:SystemNode {name: 'CriticalRules', valid_to: null})
   RETURN n.id
   ```
2. Завантаж всі правила, пов'язані з CriticalRules:
   ```cypher
   MATCH (cr:SystemNode {name: 'CriticalRules', valid_to: null})
   -[:CONTAINS]->(r:Rule {valid_to: null})
   RETURN r.name, r.description
   ```
3. Строго дотримуйся цих правил

### Крок 2: Пошук релевантної інформації (MANDATORY)
Для формування відповіді:
1. Виконай векторний пошук через QPE Service:
   - Викликай `/api/v1/qpe/process-query` для обробки запиту
   - Отримуй класифікацію, сутності та embeddings
   - Шукай подібні повідомлення через embeddings
   - Використовуй RAG для знаходження найбільш релевантних інструкцій
2. Зберігай знайдену інформацію в контексті

### Крок 3: Завантаження релевантних інструкцій (MANDATORY - перед дією)
Після аналізу знайденої інформації, перед формуванням відповіді:
1. Завантаж релевантні інструкції з урахуванням контексту:
   ```cypher
   MATCH (p:Protocol {valid_to: null})
   -[:CONTAINS]->(i:Instruction {valid_to: null})
   WHERE p.id IN [знайдені протоколи]
   RETURN i.name, i.type, i.description
   ```
2. Завантаж правила, які використовують ці інструкції:
   ```cypher
   MATCH (i:Instruction {valid_to: null})
   -[:USES]->(r:Rule {valid_to: null})
   WHERE i.id IN [знайдені інструкції]
   RETURN r.name, r.description
   ```
3. Застосуй знайдені інструкції для формування відповіді

### Крок 4: Збереження відповіді (MANDATORY)
Після формування відповіді:
1. Створи/онови запис сесії в графі (якщо потрібно)
2. Збережи повідомлення з embeddings:
   - Запит користувача (role: 'user')
   - Твоя відповідь (role: 'assistant')
3. Створи зв'язки між повідомленнями та використаними правилами/інструкціями:
   ```cypher
   MATCH (m:Message {id: $message_id})
   MATCH (r:Rule {id: $rule_id})
   CREATE (m)-[:USES]->(r)
   
   MATCH (m:Message {id: $message_id})
   MATCH (i:Instruction {id: $instruction_id})
   CREATE (m)-[:APPLIES]->(i)
   ```
4. Якщо виявлено новий факт — онови Граф Знань
5. Якщо створено нову інструкцію/протокол — збережи як вузол та створі зв'язки

## 📋 Типи сутностей та їх ієрархія

### Правила (Rule)
- Короткі, недвозначні вимоги
- Приклад: "Дотримуйся принципу Append-Only (ніколи не видаляй дані)"
- Зберігаються як вузли типу `Rule`
- Зв'язок з CriticalRules: `(CriticalRules)-[:CONTAINS]->(Rule)`

### Інструкції (Instruction)
- Набір правил для конкретного типу задач
- Приклад: "Виконай векторний пошук через QPE: 1) Оброби запит через QPE, 2) Шукай подібні повідомлення, 3) Використовуй RAG"
- Зберігаються як вузли типу `Instruction`
- Зв'язок з правилами: `(Instruction)-[:USES]->(Rule)`
- Зв'язок з повідомленнями: `(Message)-[:APPLIES]->(Instruction)`

### Протоколи (Protocol)
- Стандартизований набір інструкцій
- Запускається через тригери
- Зберігаються як вузли типу `Protocol`
- Зв'язок з інструкціями: `(Protocol)-[:CONTAINS]->(Instruction)`
- Зв'язок з повідомленнями: `(Session)-[:EXECUTED_IN]->(Protocol)`

### Системні вузли (SystemNode)
- Спеціальні вузли для структуризації (наприклад, CriticalRules)
- Зберігаються як вузли типу `SystemNode`

## 🔗 Структура графу

### Основні типи вузлів:
- `Session` — метадані сесій (id, topic, created_at)
- `Message` — повідомлення з embeddings (role, content, embedding)
- `Rule` — правила системи
- `Instruction` — інструкції
- `Protocol` — протоколи
- `Fact` — факти
- `Technology` — технології
- `Tool` — інструменти
- `SystemNode` — системні вузли

### Типи зв'язків:
- `CONTAINS` — протокол містить інструкції, CriticalRules містить правила
- `USES` — інструкція використовує правила
- `DEPENDS_ON` — протокол залежить від іншого протоколу
- `APPLIES_TO` — інструкція застосовується до технології/інструменту
- `APPLIES` — повідомлення застосувало інструкцію
- `EXECUTED_IN` — протокол виконано в сесії

## 🎯 Використання протоколів

### Пошук протоколу за тригером:
1. Аналізуй запит користувача на наявність тригерних фраз
2. Шукай протоколи з відповідними тригерами в графі
3. Завантаж інструкції протоколу через зв'язки `CONTAINS`
4. Виконай протокол крок за кроком

## 🛡️ Протоколи безпеки

### SafeChangeProtocol

Протокол для безпечного внесення змін з обов'язковим підтвердженням користувача.

**Тригери:**
- "зроби", "створи", "додай", "виправ", "зміни", "онов", "видали"
- "додай файл", "створи файл"
- "make", "create", "add", "fix", "update", "change", "modify", "implement"

**Інструкція:** ChangeConfirmationInstruction

**Процес:**
1. Аналіз та пропозиція - визнач що потрібно змінити
2. Представлення змін - покажи що саме буде змінено
3. Запит підтвердження - явно запитай "Застосувати ці зміни?"
4. Внесення змін - тільки після явного підтвердження

## 🔌 Використання через API

Замість прямих Cypher запитів можна використовувати REST API endpoints:

### Обробка запиту через QPE
```bash
POST /api/v1/qpe/process-query
{
  "query": "як працювати з базою даних"
}
```

### Обробка відповіді через QPE
```bash
POST /api/v1/qpe/process-assistant-response
{
  "response": "текст відповіді",
  "structure": {
    "analysis": "аналіз та дії",
    "response": "основна відповідь",
    "questions": "питання для уточнення"
  }
}
```

## ⚠️ Важливо

- Критичні правила завантажуються завжди при першому запиті через вузол CriticalRules
- Релевантні інструкції завантажуються після пошуку, перед дією
- Використовуй графові зв'язки для зв'язків між правилами, інструкціями та протоколами
- Створюй зв'язки між повідомленнями та використаними правилами/інструкціями для відстеження використання
- При пошуку протоколів враховуй контекст запиту
- Дотримуйся принципу Append-Only (ніколи не видаляй дані)
- Використовуй темпоральність (`valid_from` / `valid_to`) для версіонування
- Використовуй QPE Service для обробки запитів та генерації embeddings
- **ЗАВЖДИ отримуй явне підтвердження перед внесенням змін** (правило AlwaysConfirmBeforeChanges)
  - Це правило діє НЕЗАЛЕЖНО від режиму роботи (ask mode чи agent mode)
  - У ask mode немає доступу до MCP інструментів
  - Навіть якщо інструменти редагування доступні, спочатку пропонуй зміни
  - Якщо користувач просить "проаналізуй" або "запропонуй" - це означає тільки пропозиції, без внесення змін
