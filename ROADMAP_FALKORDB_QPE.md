# Дорожня карта: Перехід на FalkorDB та реалізація QPE

**Дата створення:** 20 січня 2026  
**Статус:** Планування  
**Версія:** 1.0

---

## 🎯 Мета проекту

Створити локальну систему пам'яті на базі FalkorDB з автоматичною обробкою запитів через Query Processing Engine (QPE) для навчання агента взаємодії з базою даних та збереження всіх етапів створення воркспейсу.

**Ключовий принцип:** Спочатку реалізувати прямий пайплайн `/db` → `/ss` для нових сесій, міграцію старих сесій виконати пізніше.

---

## 🔄 Пайплайн роботи

### Команда `/db [запит]`
1. Агент отримує запит користувача
2. Агент викликає QPE Service `/api/v1/qpe/process-query`
3. QPE обробляє запит:
   - Класифікація (DeBERTa): Sentiment, Intent, Complexity
   - Вилучення сутностей (GLINER): плоский список типів
   - Генерація embeddings (EmbGEMMA)
4. QPE повертає структурований контекст
5. Агент формує відповідь на основі контексту

### Команда `/ss`
1. Агент формує відповідь користувачу
2. Агент створює `.md` файл у `saved_sessions/` (тимчасовий костиль)
3. Агент викликає скрипт `save_to_falkordb.py` з шляхом до файлу
4. Скрипт парсить `.md` файл:
   - Для кожного запиту користувача: викликає QPE `/process-query`
   - Для кожної відповіді агента: викликає QPE `/process-assistant-response`
5. Скрипт створює вузли та зв'язки в FalkorDB
6. Підтвердження успішного збереження

---

## 📋 Етапи реалізації

### Етап 1: Підготовка інфраструктури FalkorDB
**Тривалість:** 1-2 дні  
**Пріоритет:** Високий  
**Статус:** ✅ Завершено

#### Задачі:
- [x] Створити Docker Compose конфігурацію з FalkorDB
- [x] Налаштувати persistence через Docker volumes
- [x] Протестувати підключення через офіційний клієнт `falkordb` (Python)
- [x] Створити базову структуру графу (маппінг з поточної PostgreSQL схеми)
- [x] Документація по підключенню та базовим операціям

#### Створені файли:
- `falkordb-service/docker-compose.yml` - Docker Compose конфігурація
- `falkordb-service/requirements.txt` - Python залежності
- `falkordb-service/scripts/test_connection.py` - Скрипт тестування підключення
- `falkordb-service/scripts/init_graph.py` - Скрипт ініціалізації структури графу
- `falkordb-service/README.md` - Документація

#### Результат:
- Працюючий Docker контейнер з FalkorDB
- Підключення через Python клієнт `from falkordb import FalkorDB`
- Базова структура графу (Session, Message, AnalysisNode, ResponseNode, QuestionsNode, Classification, Entity, EntityType)

#### Технічні деталі:
```python
# Приклад підключення
from falkordb import FalkorDB

client = FalkorDB(host="localhost", port=6379, password="your-password")
graph = client.select_graph('agent_memory')
```

---

### Етап 2: QPE Service - Базова структура
**Тривалість:** 2-3 дні  
**Пріоритет:** Високий  
**Статус:** ✅ Завершено

#### Задачі:
- [x] Створити Docker Compose конфігурацію для QPE Service
- [x] Створити базову структуру сервісу (FastAPI)
- [x] Endpoint `/api/v1/qpe/process-query`:
  - Приймає: `{"query": "текст запиту користувача"}`
  - Повертає: структурований JSON з результатами обробки
- [x] Endpoint `/api/v1/qpe/process-assistant-response`:
  - Приймає: `{"response": "текст відповіді агента", "structure": {"analysis": "...", "response": "...", "questions": "..."}}`
  - Повертає: структурований JSON з результатами обробки
- [x] Базова структура даних для результатів
- [x] Health check endpoint `/api/v1/qpe/health`

#### Створені файли:
- `falkordb-service/docker-compose.yml` - оновлено (додано сервіси `ollama` та `qpe-service`)
- `falkordb-service/Dockerfile` - Docker образ для QPE Service
- `falkordb-service/app/main.py` - FastAPI додаток
- `falkordb-service/app/config.py` - налаштування сервісу
- `falkordb-service/app/embedding.py` - сервіс генерації embeddings через Ollama
- `falkordb-service/app/api/routes.py` - QPE endpoints
- `falkordb-service/app/models/request.py` - Pydantic моделі для запитів
- `falkordb-service/app/models/response.py` - Pydantic моделі для відповідей
- `falkordb-service/requirements.txt` - оновлено (FastAPI, uvicorn, httpx, pydantic, falkordb)
- `falkordb-service/README.md` - оновлено з документацією QPE Service

#### Результат:
- ✅ QPE Service запускається в Docker (порт 8001)
- ✅ Три сервіси працюють: FalkorDB, Ollama, QPE Service
- ✅ Два endpoints готові до інтеграції компонентів:
  - `POST /api/v1/qpe/process-query` - обробка запитів користувача
  - `POST /api/v1/qpe/process-assistant-response` - обробка відповідей агента
- ✅ Health check endpoint `/api/v1/qpe/health` працює
- ✅ Базова структура даних визначена (Pydantic моделі)
- ✅ Embeddings генеруються через Ollama (модель `embeddinggemma:latest`)
- ✅ Mock-функції для класифікації та вилучення сутностей (будуть замінені в Етапах 3-4)

#### Структура відповідей QPE:

**Для запитів користувача (`/process-query`):**
```json
{
  "query": "оригінальний запит",
  "classifications": {
    "sentiment": "neutral|positive_feedback|negative_feedback|frustrated",
    "intents": ["information_seeking", "task_execution", ...],
    "complexity": "simple_question|structured_prompt|architectural|requires_clarification"
  },
  "entities": [
    {"text": "FalkorDB", "type": "Technology", "start": 0, "end": 8},
    {"text": "QPE", "type": "Component", "start": 15, "end": 18}
  ],
  "embedding": [0.123, 0.456, ...]
}
```

**Для відповідей агента (`/process-assistant-response`):**
```json
{
  "response": "оригінальна відповідь",
  "structure": {
    "analysis": "текст аналізу та дій",
    "response": "основна відповідь",
    "questions": "питання для уточнення (опціонально)"
  },
  "response_type": "explanation|code_proposal|analysis|question",
  "complexity": "simple|detailed|architectural",
  "analysis_entities": [
    {"text": "read_file", "type": "Tool", "start": 0, "end": 8},
    {"text": "ROADMAP.md", "type": "File", "start": 15, "end": 25}
  ],
  "response_entities": [
    {"text": "FalkorDB", "type": "Technology", "start": 0, "end": 8},
    {"text": "графова база", "type": "Concept", "start": 20, "end": 32}
  ],
  "embeddings": {
    "analysis": [0.123, ...],
    "response": [0.456, ...],
    "questions": [0.789, ...] // якщо є
  }
}
```

---

### Етап 3: Інтеграція DeBERTa v3 для класифікації
**Тривалість:** 3-4 дні  
**Пріоритет:** Високий  
**Статус:** ✅ Завершено

#### Задачі:
- [x] Встановити `transformers` та `torch`
- [x] Завантажити DeBERTa v3 модель (`cross-encoder/nli-deberta-v3-base`) ✅ **Виправлено:** змінено на zero-shot сумісну модель
- [x] Створити три окремі класифікатори:
  - `SentimentClassifier` (4 класи: `neutral`, `positive_feedback`, `negative_feedback`, `frustrated`)
  - `IntentClassifier` (6 класів, multi-label: `information_seeking`, `capability_inquiry`, `task_execution`, `project_discussion`, `error_resolution`, `clarification_needed`)
  - `ComplexityClassifier` (4 класи: `simple_question`, `structured_prompt`, `architectural`, `requires_clarification`)
- [x] Створити класифікатор для відповідей агента:
  - `ResponseTypeClassifier` (4 класи: `explanation`, `code_proposal`, `analysis`, `question`)
  - `ResponseComplexityClassifier` (3 класи: `simple`, `detailed`, `architectural`)
- [x] Реалізувати zero-shot підхід (без fine-tuning на початку)
- [x] Інтегрувати класифікатори в QPE Service
- [x] Тестування на прикладах запитів та відповідей

#### Створені файли:
- `falkordb-service/app/classification.py` - модуль з усіма класифікаторами
- `falkordb-service/requirements.txt` - оновлено (додано transformers, torch, accelerate, sentencepiece, **protobuf>=3.20.0** ✅)
- `falkordb-service/Dockerfile` - оновлено (додано системні залежності для torch)
- `falkordb-service/docker-compose.yml` - оновлено (збільшено start_period для завантаження моделей)
- `falkordb-service/app/api/routes.py` - оновлено (замінено mock-функції на реальні класифікатори)

#### Результат:
- ✅ П'ять класифікаторів працюють (три для запитів, два для відповідей)
- ✅ Zero-shot класифікація через `cross-encoder/nli-deberta-v3-base` pipeline ✅ **Виправлено:** використовується правильна zero-shot модель
- ✅ Результати інтегровані в QPE endpoints (`/process-query` та `/process-assistant-response`)
- ✅ Обробка помилок з fallback на значення за замовчуванням
- ✅ Lazy loading моделей (singleton pattern)
- ✅ Підтримка GPU (автоматично через transformers)
- ✅ **Виправлено:** Додано `protobuf>=3.20.0` для коректної роботи transformers

#### Виправлені проблеми (20 січня 2026):
1. ✅ **КРИТИЧНО:** Додано `protobuf>=3.20.0` до requirements.txt - виправлено помилку завантаження класифікаторів
2. ✅ **КРИТИЧНО:** Змінено модель з `microsoft/deberta-v3-base` на `cross-encoder/nli-deberta-v3-base` - тепер використовується правильна zero-shot сумісна модель
3. ✅ Підтверджено успішну роботу: всі класифікатори завантажуються та працюють коректно
4. ✅ Протестовано на реальних даних: API повертає правильні результати класифікації

#### Технічні деталі:
```python
# Приклад структури класифікаторів
class SentimentClassifier:
    def classify(self, text: str) -> str:
        # Zero-shot класифікація
        pass

class IntentClassifier:
    def classify(self, text: str) -> List[str]:
        # Multi-label класифікація
        pass
```

---

### Етап 4: Інтеграція GLINER v2.1 для вилучення сутностей
**Тривалість:** 2-3 дні  
**Пріоритет:** Високий  
**Статус:** Не розпочато

#### Задачі:
- [ ] Встановити GLINER v2.1
- [ ] Створити плоский список типів сутностей для запитів користувача:
  ```python
  QUERY_ENTITY_TYPES = [
      "Technology", "Framework", "Library", "Database", "Language",
      "Project", "Component", "Service", "API", "Endpoint",
      "Task", "Feature", "Bug", "Requirement",
      "Concept", "Pattern", "Architecture", "Design",
      "Person", "Role", "Team",
      "File", "Directory", "Config", "Script",
      "Preference", "Constraint", "Decision"
  ]
  ```
- [ ] Створити розширений список типів сутностей для відповідей агента:
  ```python
  RESPONSE_ENTITY_TYPES = QUERY_ENTITY_TYPES + [
      "Tool",           # read_file, codebase_search, тощо
      "File",           # конкретні файли
      "CodeBlock",      # блоки коду
      "Decision",       # прийняті рішення
      "Recommendation", # рекомендації
      "Action",         # виконані дії
      "Analysis",       # аналіз ситуації
      "Question"       # питання для уточнення
  ]
  ```
- [ ] Реалізувати `EntityExtractor` з використанням GLINER:
  - Для запитів: використовує `QUERY_ENTITY_TYPES`
  - Для відповідей: використовує `RESPONSE_ENTITY_TYPES`
- [ ] Інтегрувати extractor в QPE Service
- [ ] Тестування на прикладах

#### Результат:
- GLINER витягує сутності з запитів та відповідей
- Різні типи сутностей для різних типів текстів
- Результати інтегровані в QPE endpoints

---

### Етап 5: Інтеграція EmbGEMMA для embeddings
**Тривалість:** 1-2 дні  
**Пріоритет:** Середній  
**Статус:** Не розпочато

#### Задачі:
- [ ] Вирішити: Ollama чи Hugging Face напряму?
  - Рекомендація: спочатку Ollama (як у поточному embedding-service), потім можна перейти на Hugging Face
- [ ] Інтегрувати генерацію embeddings в QPE Service:
  - Для запитів: один embedding
  - Для відповідей: окремі embeddings для analysis, response, questions
- [ ] Налаштувати підключення до Ollama (якщо використовуємо)
- [ ] Тестування генерації embeddings

#### Результат:
- Embeddings генеруються для запитів та відповідей
- Для відповідей генеруються окремі embeddings для кожної частини

---

### Етап 6: Скрипт збереження в FalkorDB
**Тривалість:** 2-3 дні  
**Пріоритет:** Високий  
**Статус:** Не розпочато

#### Задачі:
- [ ] Створити скрипт `save_to_falkordb.py`:
  - Приймає шлях до `.md` файлу сесії
  - Парсить структуру сесії (запити користувача + відповіді агента)
  - Для кожного запиту користувача:
    - Викликає QPE `/api/v1/qpe/process-query`
    - Створює вузол `Message` (role: 'user')
    - Створює вузол `Classification` з результатами класифікації
    - Створює вузли `Entity` та `EntityType`
    - Створює зв'язки між вузлами
  - Для кожної відповіді агента:
    - Викликає QPE `/api/v1/qpe/process-assistant-response`
    - Створює вузол `Message` (role: 'assistant')
    - Створює вузли `AnalysisNode`, `ResponseNode`, `QuestionsNode` (якщо є)
    - Створює вузли `Classification` для відповіді
    - Створює вузли `Entity` для кожної частини
    - Створює зв'язки між вузлами
- [ ] Структура графу в FalkorDB:
  ```
  Session
    ├─ Message (user)
    │   ├─ Classification (sentiment, intent, complexity)
    │   └─ Entity → EntityType
    └─ Message (assistant)
        ├─ AnalysisNode
        │   ├─ ActionNode
        │   └─ Entity → EntityType
        ├─ ResponseNode
        │   └─ Entity → EntityType
        ├─ QuestionsNode (опціонально)
        │   └─ Entity → EntityType
        └─ Classification (response_type, complexity)
  ```
- [ ] Обробка помилок та логування
- [ ] Тестування на прикладах сесій

#### Результат:
- Скрипт зберігає сесії у FalkorDB з повною структурою
- Всі зв'язки створюються коректно
- Можливість збереження нових сесій

#### Приклад використання:
```bash
python scripts/save_to_falkordb.py --file saved_sessions/session_2026-01-20_falkordb_planning.md
```

---

### Етап 7: Інтеграція команд `/db` та `/ss` з агентом
**Тривалість:** 2-3 дні  
**Пріоритет:** Високий  
**Статус:** Не розпочато

#### Задачі:
- [ ] Реалізувати обробку команди `/db [запит]`:
  - Агент розпізнає команду `/db`
  - Викликає QPE Service `/api/v1/qpe/process-query` з запитом
  - Отримує структурований контекст
  - Формує відповідь на основі контексту
  - Відображає відповідь користувачу
- [ ] Реалізувати обробку команди `/ss`:
  - Агент розпізнає команду `/ss`
  - Створює `.md` файл у `saved_sessions/` з поточною сесією
  - Викликає скрипт `save_to_falkordb.py` з шляхом до файлу
  - Очікує завершення збереження
  - Підтверджує успішне збереження користувачу
- [ ] Створити документацію для агента про використання команд
- [ ] Тестування повного пайплайну

#### Результат:
- Команди `/db` та `/ss` працюють повністю
- Агент може використовувати QPE для отримання контексту
- Агент може зберігати сесії в FalkorDB
- Повний пайплайн працює від запиту до збереження

---

### Етап 8: Тестування та оптимізація
**Тривалість:** 2-3 дні  
**Пріоритет:** Середній  
**Статус:** Не розпочато

#### Задачі:
- [ ] Протестувати всі компоненти разом
- [ ] Перевірити продуктивність QPE (час обробки)
- [ ] Оптимізувати запити до FalkorDB (Cypher queries)
- [ ] Додати обробку помилок та retry логіку
- [ ] Додати логування для відстеження процесу
- [ ] Створити документацію:
  - Як використовувати команди `/db` та `/ss`
  - Як працює QPE Service
  - Структура даних у FalkorDB
  - Приклади запитів Cypher для витягування даних

#### Результат:
- Стабільна робота всієї системи
- Документація для подальшого використання
- Оптимізована продуктивність

---

### Етап 9: Міграція існуючих сесій (пізніше)
**Тривалість:** 1-2 дні  
**Пріоритет:** Низький  
**Статус:** Не розпочато

#### Задачі:
- [ ] Створити скрипт міграції для всіх `.md` файлів з `saved_sessions/`
- [ ] Прогнати кожну сесію через QPE (якщо потрібно)
- [ ] Зберегти всі сесії у FalkorDB
- [ ] Перевірити цілісність даних
- [ ] Порівняти дані до та після міграції

#### Результат:
- Всі існуючі сесії мігровані у FalkorDB
- Створено контент для експериментів

---

## 🔧 Технічний стек

### База даних:
- **FalkorDB** - графова база даних (Docker)
- **falkordb** (Python) - офіційний Python клієнт
- **Cypher** - мова запитів для графу

### QPE Service:
- **Python 3.11+**
- **FastAPI** - REST API
- **transformers** - DeBERTa v3
- **torch** - для моделей машинного навчання
- **gliner** - GLINER v2.1 для вилучення сутностей
- **ollama** або **sentence-transformers** - для EmbGEMMA embeddings

### Інфраструктура:
- **Docker Compose** - оркестрація сервісів
- **Docker volumes** - persistence для FalkorDB

---

## 📊 Структура даних у FalkorDB

### Вузли (Nodes):

#### Базові вузли:
- **Session** - сесія
  - `id` (UUID)
  - `topic` (string)
  - `created_at` (timestamp)
  - `metadata` (JSON)

- **Message** - повідомлення
  - `id` (UUID)
  - `role` ('user' | 'assistant')
  - `content` (string, повний текст)
  - `embedding` (vector, для user messages)
  - `created_at` (timestamp)

#### Вузли для відповідей агента:
- **AnalysisNode** - аналіз та дії
  - `id` (UUID)
  - `content` (string)
  - `embedding` (vector)

- **ResponseNode** - основна відповідь
  - `id` (UUID)
  - `content` (string)
  - `embedding` (vector)

- **QuestionsNode** - питання для уточнення (опціонально)
  - `id` (UUID)
  - `content` (string)
  - `embedding` (vector)

- **ActionNode** - окрема дія з аналізу
  - `id` (UUID)
  - `content` (string)
  - `tool_name` (string, опціонально)
  - `result` (string, опціонально)

#### Класифікація:
- **Classification** - класифікація запиту
  - `id` (UUID)
  - `sentiment` (string)
  - `intents` (array of strings)
  - `complexity` (string)

- **ResponseClassification** - класифікація відповіді
  - `id` (UUID)
  - `response_type` (string)
  - `complexity` (string)

#### Сущності:
- **Entity** - вилучена сутність
  - `id` (UUID)
  - `text` (string)
  - `start` (int)
  - `end` (int)

- **EntityType** - тип сутності
  - `id` (UUID)
  - `name` (string): "Technology", "Tool", "File", тощо

#### Класифікаційні вузли (для зв'язування):
- **Sentiment** - вузол для sentiment класу
  - `name`: "neutral", "positive_feedback", "negative_feedback", "frustrated"

- **Intent** - вузол для intent класу
  - `name`: "information_seeking", "task_execution", тощо

- **Complexity** - вузол для complexity класу
  - `name`: "simple_question", "structured_prompt", тощо

- **ResponseType** - вузол для типу відповіді
  - `name`: "explanation", "code_proposal", "analysis", "question"

### Зв'язки (Relationships):

#### Базові зв'язки:
- `(Session)-[:CONTAINS]->(Message)`
- `(Message)-[:HAS_CLASSIFICATION]->(Classification)` (для user messages)
- `(Message)-[:HAS_RESPONSE_CLASSIFICATION]->(ResponseClassification)` (для assistant messages)
- `(Message)-[:HAS_ENTITY]->(Entity)`
- `(Entity)-[:IS_TYPE]->(EntityType)`

#### Зв'язки для відповідей агента:
- `(Message)-[:CONTAINS]->(AnalysisNode)`
- `(Message)-[:CONTAINS]->(ResponseNode)`
- `(Message)-[:CONTAINS]->(QuestionsNode)` (опціонально)
- `(AnalysisNode)-[:HAS_ACTION]->(ActionNode)`
- `(ActionNode)-[:USED_TOOL]->(Entity)` (якщо використовував інструмент, Entity з типом "Tool")
- `(ActionNode)-[:READ_FILE]->(Entity)` (якщо читав файл, Entity з типом "File")
- `(AnalysisNode)-[:HAS_ENTITY]->(Entity)`
- `(ResponseNode)-[:HAS_ENTITY]->(Entity)`
- `(QuestionsNode)-[:HAS_ENTITY]->(Entity)` (якщо є)

#### Зв'язки для класифікації:
- `(Classification)-[:HAS_SENTIMENT]->(Sentiment)`
- `(Classification)-[:HAS_INTENT]->(Intent)` (може бути кілька)
- `(Classification)-[:HAS_COMPLEXITY]->(Complexity)`
- `(ResponseClassification)-[:HAS_RESPONSE_TYPE]->(ResponseType)`
- `(ResponseClassification)-[:HAS_COMPLEXITY]->(Complexity)`

#### Зв'язки для пошуку подібних:
- `(Message)-[:SIMILAR_TO]->(Message)` (через embeddings, якщо підтримується)

---

## 🎯 Критерії успіху

1. ✅ FalkorDB запускається локально через Docker
2. ✅ QPE Service обробляє запити користувача та відповіді агента
3. ✅ Команда `/db` працює: агент отримує контекст через QPE
4. ✅ Команда `/ss` працює: агент зберігає сесії у FalkorDB
5. ✅ Класифікація працює (три виміри для запитів, два для відповідей)
6. ✅ Вилучення сутностей працює (GLINER з різними типами для запитів та відповідей)
7. ✅ Embeddings генеруються та зберігаються
8. ✅ Структура графу коректна (всі зв'язки створюються)
9. ✅ Система стабільно працює
10. ✅ Документація створена

---

## 📝 Нотатки та обмеження

### Тимчасові костилі:
- Команди `/db` та `/ss` - тимчасові костилі для контролю та навчання
- Збереження `.md` файлів - тимчасовий костиль для бекапу
- В майбутньому: автоматична обробка без команд, пряме збереження в БД

### Масштабування:
- Початок: три виміри класифікації + плоский список сутностей
- Розширення: додавання нових класів та типів сутностей на основі даних
- Оптимізація: групування сущностей в ієрархії на основі накопичених даних

### Використання моделей:
- На початку: використовувати моделі без fine-tuning
- В майбутньому: можливий fine-tuning на накопичених даних

### Персистентність:
- FalkorDB зберігає дані в Docker volumes
- Регулярні бекапи рекомендовані
- `.md` файли залишаються як додатковий бекап (тимчасово)

---

## 🔍 Приклади використання

### Приклад 1: Обробка запиту користувача

**Запит:**
```
/db Як зберегти сесію у FalkorDB?
```

**Процес:**
1. Агент викликає `POST /api/v1/qpe/process-query`
2. QPE повертає:
   ```json
   {
     "classifications": {
       "sentiment": "neutral",
       "intents": ["information_seeking"],
       "complexity": "simple_question"
     },
     "entities": [
       {"text": "FalkorDB", "type": "Database"},
       {"text": "сесія", "type": "Concept"}
     ],
     "embedding": [...]
   }
   ```
3. Агент формує відповідь на основі контексту

### Приклад 2: Збереження відповіді агента

**Відповідь агента:**
```markdown
### Аналіз та дії

1. Прочитано документацію FalkorDB
2. Проаналізовано структуру проекту

### Відповідь

Для збереження сесії у FalkorDB потрібно...
```

**Процес:**
1. Агент створює `.md` файл
2. Агент викликає `save_to_falkordb.py`
3. Скрипт викликає `POST /api/v1/qpe/process-assistant-response`
4. QPE повертає структуровані дані
5. Скрипт створює вузли та зв'язки в FalkorDB

---

## 📚 Додаткові ресурси

- [FalkorDB Documentation](https://docs.falkordb.com/)
- [FalkorDB Python Client](https://github.com/FalkorDB/falkordb-py)
- [Cross-Encoder NLI DeBERTa v3 Model](https://huggingface.co/cross-encoder/nli-deberta-v3-base) (використовується для zero-shot класифікації)
- [GLINER v2.1](https://github.com/urchade/gliner)
- [EmbGEMMA](https://ollama.com/library/embeddinggemma)

---

**Останнє оновлення:** 20 січня 2026  
**Автор:** AI Agent + User  
**Версія документа:** 1.0
