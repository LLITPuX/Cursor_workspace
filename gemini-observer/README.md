# Gemini-Observer

Телеграм-агент на базі моделі Gemini.

📘 **Технічна документація:** [ARCHITECTURE.md](../ARCHITECTURE.md)


## 🚀 Швидкий Старт (Docker)

Вам не потрібно встановлювати Python. Все працює через Docker.

### 1. Отримання Ключів Telegram

#### А. Отримання Токена Бота (`TELEGRAM_BOT_TOKEN`)
1.  Відкрийте Telegram і знайдіть бота **@BotFather**.
2.  Напишіть команду `/newbot`.
3.  Дотримуйтесь інструкцій:
    -   Введіть ім'я бота (наприклад: `MyGeminiObserver`).
    -   Введіть юзернейм (має закінчуватися на `bot`, наприклад: `my_gemini_observer_bot`).
4.  Copy the token (Format: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`).

#### Б. Отримання Вашого ID (`ALLOWED_USER_IDS`)
1.  Знайдіть у Telegram бота **@userinfobot**.
2.  Натисніть `/start`.
3.  Скопіюйте число `Id`.

### 2. Отримання Ключів Google (`client_secret.json`)

Вам потрібно завантажити файл авторизації від Google Cloud.

1.  **Відкрийте Google Cloud Console**: [https://console.cloud.google.com/](https://console.cloud.google.com/)
2.  **Створіть Проект**:
    -   Натисніть на список проектів зверху зліва.
    -   **"New Project"** -> Назвіть `Gemini-Observer` -> **"Create"**.
3.  **Увімкніть API**:
    -   У пошуку зверху введіть: `Generative Language API`.
    -   Натисніть **"Enable"**.
4.  **Налаштуйте Екран Згоди (Consent Screen)**:
    -   **"APIs & Services"** -> **"OAuth consent screen"**.
    -   Виберіть **"External"** -> "Create".
    -   **App Info**: Name: `Gemini Observer`, User support email: ваша пошта.
    -   **Developer Contact**: ваша пошта.
    -   "Save and Continue" (пропустіть Scopes/Users).
    -   **ВАЖЛИВО**: У Dashboard натисніть **"PUBLISH APP"**.
5.  **Створіть Ключі (Credentials)**:
    -   **"APIs & Services"** -> **"Credentials"**.
    -   **"+ CREATE CREDENTIALS"** -> **"OAuth client ID"**.
    -   **App type**: **"Desktop app"**.
    -   Name: `Gemini Desktop Client` -> "Create".
6.  **Завантажте**:
    -   Натисніть **"DOWNLOAD JSON"** ⬇️.
    -   Перейменуйте файл у `client_secret.json`.
    -   Покладіть його в папку **`credentials/`** всередині проекту.

### 3. Налаштування Конфігурації

1.  Відкрийте файл `.env`.
2.  Заповніть змінні:
    ```ini
    TELEGRAM_BOT_TOKEN=ваш_токен
    ALLOWED_USER_IDS=[123456789]
    GEMINI_CLIENT_SECRET_PATH=credentials/client_secret.json
    GEMINI_TOKEN_PATH=credentials/token.json
    ```

### 4. Запуск

Запустіть проект:

```bash
docker-compose up --build
```
> **ВАЖЛИВО**: Не використовуйте прапорець `-d` (detached mode) для першого запуску! Ви повинні бачити логи консолі, щоб отримати посилання для входу.

### Перший Запуск (Аутентифікація)

При першому запуску контейнер попросить увійти:

1.  У консолі з'явиться довге посилання `https://accounts.google.com/...`.
2.  **Відкрийте це посилання у браузері**.
3.  Увійдіть у Google і натисніть "Allow".
4.  Якщо аутентифікація пройшла успішно, ви побачите повідомлення "Authentication successful".
    - *Якщо сторінка не відкривається (localhost), скопіюйте код авторизації, якщо він буде запитаний.*
5.  Бот автоматично продовжить запуск.
