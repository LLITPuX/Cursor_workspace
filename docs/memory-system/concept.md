# Концепція Темпоральної Пам'яті (Temporal Memory Concept)

## 📜 Філософія: Конституція Пам'яті
Ми будуємо систему пам'яті, яка працює за принципом **Append-Only Event Sourcing**. 
1. **Нічого не видаляється.** Історія священна. Помилки не стираються, а виправляються новими записами.
2. **Темпоральність (Час існування).** Кожен факт має час, коли він був істинним.
   - Ми завжди можемо спитати: "Що ми знали про проект X станом на минулий вівторок?"
3. **Гілкування (Branching).** Кожен новий проект — це відгалуження (Neon Branch) від основного стовбура знань (`main`).

## 🏗️ Архітектура

### 1. Raw Logs (Непорушні Логи)
- Повний, дослівний запис усіх сесій.
- Зберігається в таблицях `sessions` та `messages`.
- **Зміни:** Неможливі. Тільки додавання нових повідомлень.

### 2. Temporal Knowledge Graph (Темпоральний Граф Знань)
Замість статичних фактів ("Docker встановлено"), ми зберігаємо факти з часовими мітками:
- `valid_from`: Дата/час, коли факт став істиною.
- `valid_to`: Дата/час, коли факт перестав бути актуальним (NULL = актуально зараз).
- `superseded_by`: ID факту, який замінив цей запис.

**Приклад:**
- *Запис 1:* "Користувач використовує Node.js v14" (`valid_from`: 2023-01-01, `valid_to`: 2023-06-01)
- *Запис 2:* "Користувач використовує Node.js v18" (`valid_from`: 2023-06-01, `valid_to`: NULL)

### 3. Neon MCP Integration
Агент (Cursor AI) взаємодіє з цією системою через набір інструментів MCP.
- **Інструменти:** Доступні агенту в режимі Agent Mode.
- **Обізнаність:** В режимі Ask Mode агент читає цю документацію, щоб розуміти свої можливості.

## 🛠️ Технологічний Стек
- **Database:** Neon (Serverless PostgreSQL).
- **Extension:** `pgvector` (для семантичного пошуку), `timescaledb` (опціонально для часових рядів).
- **Protocol:** MCP (Model Context Protocol).

