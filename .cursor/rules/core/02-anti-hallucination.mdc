---
description: Протокол верифікації — захист від галюцинацій та помилок AI.
globs: ["**/*"]
alwaysApply: true
---

# 🛡️ Протокол захисту від галюцинацій

AI-моделі можуть "вигадувати" факти з повною впевненістю. Цей протокол мінімізує такі помилки.

## ⚠️ Що таке галюцинації?

> **Для користувача:**
> Галюцинація — це коли AI впевнено стверджує щось, що не є правдою. Наприклад:
> - Вигадує неіснуючі функції бібліотек
> - Посилається на файли яких немає
> - Придумує "факти" про технології
> 
> Це не злий намір — так працюють мовні моделі. Вони генерують "найбільш ймовірний" текст, а не "найбільш правдивий".

## 🔍 Правила для AI

### 1. Перевірка перед відповіддю
- **Файли:** Перед згадуванням файлу — перевір що він існує (`read_file`, `list_dir`)
- **Код:** Перед описом функції — знайди її в коді (`grep`, `codebase_search`)
- **Зовнішні факти:** Якщо не впевнений — скажи це прямо

### 2. Чесність про невпевненість
Використовуй фрази:
- "Я не впевнений, але припускаю що..."
- "Потрібно перевірити, але схоже що..."
- "Не знаходжу підтвердження, можливо..."

**ЗАБОРОНЕНО:** Видавати припущення за факти.

### 3. Пошук перед відповіддю
При пошуку інформації в проекті:

```
Алгоритм:
1. Шукай точний збіг (grep -i "term")
2. Шукай синоніми та варіації
3. Перевіряй контекст знайденого
4. Якщо нічого не знайдено — скажи це
```

### 4. Пріоритет джерел
```
1. docs/active_context/*  → Поточна істина
2. Код проекту            → Факти реалізації
3. docs/sessions/*        → Історія (може бути застарілою)
4. Зовнішні знання        → Потребує верифікації
```

### 5. Конфлікти інформації
Якщо знайдено суперечливу інформацію:
- **Повідом користувача** про конфлікт
- Вкажи джерела
- Запитай що актуально

## 🚨 Червоні прапорці (для користувача)

Будь насторозі якщо AI:
- Дуже впевнено описує щось чого ти не бачив
- Посилається на файли/функції без їх показу
- Дає "факти" про нові технології (може бути застаріла інформація)
- Не може показати джерело інформації

## ✅ Як перевірити AI

Якщо сумніваєшся:
1. Попроси показати конкретний файл/код
2. Попроси пояснити звідки ця інформація
3. Погугли критичні факти самостійно
4. Запитай: "Ти впевнений? Перевір ще раз."
