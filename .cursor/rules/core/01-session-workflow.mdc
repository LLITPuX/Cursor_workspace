---
description: Протокол сесій — 4-фазний цикл роботи для збереження контексту та структурованої розробки.
globs: ["**/*"]
alwaysApply: true
---

# 📋 Протокол робочих сесій

Цей протокол вирішує проблему "забування" — AI не пам'ятає попередні розмови, тому кожна сесія має бути задокументована.

## 🔄 4 фази роботи

### Фаза 1: ОБГОВОРЕННЯ (Ask Mode)
**Мета:** Зрозуміти задачу перед тим як щось робити.

- Задавай уточнювальні питання
- Оціни ризики та складність
- Критикуй ідею (конструктивно) — виділи потенційні проблеми
- **Результат:** Чіткий план дій
- ⛔ **НЕ пиши код** поки план не затверджено користувачем

### Фаза 2: ДІЯ (Agent Mode)
**Мета:** Реалізувати затверджений план.

- Виконуй план крок за кроком
- Перед редагуванням важливих файлів — питай дозвіл
- Перед операціями з базою даних — питай дозвіл
- Перевіряй свою роботу перед звітуванням

### Фаза 3: ОГЛЯД (User Feedback)
**Мета:** Користувач перевіряє результат.

- Чекай на зворотний зв'язок
- Приймай критику як вхідні дані для покращення
- Якщо щось не так — повертайся до Фази 2

### Фаза 4: АРХІВАЦІЯ
**Тригер:** Користувач каже "Archive session", "Заверши сесію", або подібне.

**Дії:**
1. Створи/онови файл сесії: `docs/sessions/YYYY-MM-DD_NNN_topic.md`
2. Запиши що було зроблено (не резюме, а факти)
3. Онови `docs/active_context/roadmap.md` якщо потрібно
4. Git commit: `[Session: YYYY-MM-DD_NNN_topic] Short description`
5. Git push

## 📁 Структура документації

```
docs/
├── sessions/              # Історія — що сталося
│   └── YYYY-MM-DD_NNN_topic.md
│
└── active_context/        # Поточний стан — що є правдою зараз
    ├── roadmap.md         # План робіт
    ├── architecture.md    # Опис системи
    └── resources.md       # Інструменти та налаштування
```

## ⚡ Важливо для користувача

> **Чому це важливо?**
> Без цієї системи кожна нова розмова — це "чистий аркуш". AI не знатиме що ви робили раніше.
> 
> Сесії — це як "зберігання гри". Завжди завершуй сесію перед закриттям, щоб прогрес не загубився.

## 🏷️ Формат назв файлів сесій

```
YYYY-MM-DD_NNN_topic.md

Де:
- YYYY-MM-DD — дата
- NNN — порядковий номер сесії за день (001, 002, ...)
- topic — коротка тема (латиницею, через-дефіс)

Приклади:
- 2025-12-04_001_project-setup.md
- 2025-12-04_002_auth-implementation.md
```
