---
description: Технічні стандарти — правила написання коду та використання технологій.
globs: ["**/*.py", "**/*.ts", "**/*.js", "**/*.tsx", "**/*.jsx", "Dockerfile", "docker-compose.yml"]
alwaysApply: true
---

# ⚙️ Технічні стандарти

## 🎯 Філософія

1. **Модульність** — кожен компонент має одну відповідальність
2. **Читабельність** — код пишеться для людей, не для машин
3. **Строгість** — краще суворі правила ніж хаос
4. **Async-first** — всі I/O операції асинхронні

## 📝 Мовні стандарти

| Що | Мова |
|----|------|
| Спілкування з AI | Українська (або мова користувача) |
| Документація проекту | Мова користувача |
| Код (змінні, функції) | Англійська |
| Коментарі в коді | Англійська |
| Git commits | Англійська |

### Чому так?

> **Для користувача:**
> Код англійською — це стандарт індустрії. Якщо колись захочеш поділитися проектом або попросити допомоги — англійський код зрозуміють всі.
> 
> Коментарі англійською — щоб код був консистентним і читабельним.

## 🐍 Python

### Обов'язково
- **PEP8** — стандарт форматування
- **Type hints** — типізація всюди
- **Async/await** — для I/O операцій
- **Docstrings** — для публічних функцій

### Приклад правильного коду
```python
from typing import Optional, List
import asyncio

async def fetch_user_data(user_id: int) -> Optional[dict]:
    """
    Fetch user data from the database.
    
    Args:
        user_id: The unique identifier of the user.
        
    Returns:
        User data dictionary or None if not found.
    """
    # Implementation here
    pass
```

### Заборонено
- `from module import *`
- Глобальні змінні без крайньої необхідності
- Синхронні I/O в async контексті

## 📘 TypeScript

### Обов'язково
- **strict: true** в tsconfig
- **ESLint** — стандарт форматування
- **Явні типи** — не покладатися на inference для публічного API

### Приклад правильного коду
```typescript
interface UserData {
  id: number;
  name: string;
  email: string;
}

async function fetchUserData(userId: number): Promise<UserData | null> {
  // Implementation here
  return null;
}
```

## 🐳 Docker

### Обов'язково для продакшену
- **Docker Compose** — для оркестрації сервісів
- **Multi-stage builds** — для оптимізації розміру
- **.dockerignore** — для виключення зайвого

### Структура docker-compose.yml
```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ./src:/app/src
```

## 📁 Структура проекту

### Python проект
```
project/
├── src/
│   ├── __init__.py
│   ├── main.py
│   └── modules/
├── tests/
├── docs/
├── requirements.txt
├── Dockerfile
└── docker-compose.yml
```

### TypeScript проект
```
project/
├── src/
│   ├── index.ts
│   └── components/
├── tests/
├── docs/
├── package.json
├── tsconfig.json
└── docker-compose.yml
```

## ⚠️ Перед редагуванням файлів

AI ЗАВЖДИ має:
1. Прочитати файл перед редагуванням
2. Запитати дозвіл перед зміною критичних файлів
3. Пропонувати бекап перед великим рефакторингом
