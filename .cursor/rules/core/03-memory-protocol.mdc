---
description: Стандарти та протоколи використання системи пам'яті Neon (Cursor Infinite Memory). Завжди активне правило.
globs: 
---
# 🧠 Протокол Пам'яті (Memory Protocol)

Ти працюєш з системою "Cursor Infinite Memory" на базі Neon MCP.

## 🔗 Основні посилання
- **Концепція:** `docs/memory-system/concept.md`
- **Протокол:** `docs/memory-system/rules/memory-protocol.md`
- **Roadmap:** `docs/memory-system/roadmap.md`

## 🚨 Ключові принципи (MUST FOLLOW)

1.  **Append-Only:** Ніколи не видаляй дані з бази (`DELETE` заборонено). Використовуй `valid_to` для архівації фактів.
2.  **Temporality:** Усі факти в Графі Знань мають час життя (`valid_from`, `valid_to`).
3.  **Branching:** Нові проекти — це нові гілки в Neon. Не засмічуй `main`.
4.  **Neon MCP:** Використовуй інструменти MCP (`run_sql`, `create_branch`) для роботи з пам'яттю.

## 🛠️ Як користуватися

### Читання контексту (Start of Session)
1.  Перевір поточний проект/гілку.
2.  Виконай SQL для отримання актуальних фактів:
    ```sql
    SELECT * FROM entity_nodes WHERE valid_to IS NULL AND type = 'Technology';
    ```

### Запис контексту (End of Session)
1.  Збережи нові повідомлення в `messages`.
2.  Онови Граф Знань, якщо з'явилися нові факти (з закриттям старих версій через `valid_to`).
