---
description: Система пам'яті — як зберігати та відновлювати контекст між сесіями.
globs: ["**/*"]
alwaysApply: true
---

# 🧠 Система пам'яті проекту

AI не має довготривалої пам'яті. Ця система компенсує це обмеження через структуровану документацію.

## 🗄️ Два типи пам'яті

### 1. Історія (sessions/)
**Що:** Записи минулих сесій — що відбулося.
**Характер:** Незмінні (append-only). Не редагуються після створення.
**Для чого:** Відстеження еволюції проекту, пошук причин рішень.

```
docs/sessions/
├── 2025-12-01_001_init.md           # Перша сесія
├── 2025-12-02_001_auth-design.md    # Обговорення авторизації
└── 2025-12-02_002_auth-implement.md # Реалізація
```

### 2. Поточний стан (active_context/)
**Що:** Актуальна інформація — що є правдою ЗАРАЗ.
**Характер:** Змінні. Оновлюються коли стан проекту змінюється.
**Для чого:** Швидке розуміння поточного стану без читання всієї історії.

```
docs/active_context/
├── roadmap.md      # Що робимо, що зроблено, що далі
├── architecture.md # Як влаштована система
└── resources.md    # Інструменти, API, налаштування
```

## 📖 Як AI використовує пам'ять

### На початку сесії
```
1. Прочитай docs/active_context/* → Зрозумій поточний стан
2. Якщо потрібно деталі → Знайди відповідну сесію в docs/sessions/
3. Ніколи не припускай → Завжди перевіряй файли
```

### Під час роботи
```
1. Тримай в голові контекст з active_context
2. Якщо щось змінюється → Запам'ятай для оновлення
3. При конфліктах → Питай користувача що актуально
```

### В кінці сесії
```
1. Запиши сесію в docs/sessions/
2. Онови docs/active_context/ якщо стан змінився
3. Commit + Push
```

## ⚠️ Важливо для користувача

> **Аналогія:**
> - `sessions/` — це як фотоальбом. Фото минулого не змінюються.
> - `active_context/` — це як дошка з нотатками. Оновлюється коли щось змінюється.
> 
> **Проблема:** Якщо не оновлювати active_context, AI буде працювати зі старою інформацією.
> 
> **Рішення:** Завжди закінчуй сесію командою архівації.

## 📝 Формат файлів active_context

### roadmap.md
```markdown
# Дорожня карта

## Поточний етап: [Назва етапу]
- [x] Завершені задачі
- [ ] Поточні задачі
- [ ] Заплановані задачі

## Беклог
- [ ] Майбутні ідеї
```

### architecture.md
```markdown
# Архітектура

## Огляд
Короткий опис системи.

## Компоненти
- Компонент A: опис
- Компонент B: опис

## Технології
- Мова: X
- База даних: Y
- Інше: Z
```

### resources.md
```markdown
# Ресурси та інструменти

## Профіль розробника
- Рівень досвіду: X
- Знайомі технології: Y

## Інструменти проекту
- API: (назва, статус налаштування)
- MCP: (що підключено)
- Інше: (...)

## Облікові записи
- GitHub: налаштовано
- OpenAI API: потрібен ключ
```

## 🔄 Команда відновлення контексту

Якщо AI "забув" контекст посеред сесії, скажи:
```
"Прочитай docs/active_context/ і продовжимо"
```
